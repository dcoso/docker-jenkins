master:
  build: .
  environment:
    JAVA_OPTS: "-Djava.awt.headless=true"
  ports:
    - "50000:50000"
    - "8080:8080"
  volumes:
    - /home/km/jenkins/home:/var/jenkins_home
    - /var/run/docker.sock:/var/run/docker.sock
